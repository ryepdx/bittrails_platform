# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "bittrails"

  config.vm.provider :aws do |aws|
    aws.access_key_id = ""
    aws.secret_access_key = ""
    aws.keypair_name = ""
    aws.ssh_private_key_path = ""

    aws.ami = ""
  end
end
